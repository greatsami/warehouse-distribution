version: "3"

services:
  redis:
    image: redis:latest
    container_name: "redis"
    ports:
      - 6379:6379
    volumes:
      - warehouse:/data
    networks:
      - warehouse

networks:
  warehouse:
    driver: bridge

volumes:
  warehouse:
    driver: local